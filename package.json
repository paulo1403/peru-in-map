{
  "name": "quehacer-pe",
  "private": true,
  "version": "0.0.1",
  "description": "QueHacer.pe - Plataforma para descubrir lugares locales en Perú",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "dev:full": "pnpm --parallel dev",
    "build": "pnpm --filter frontend build",
    "build:backend": "pnpm --filter backend build",
    "build:all": "pnpm --parallel build",
    "test": "pnpm --filter frontend test",
    "test:backend": "pnpm --filter backend test",
    "test:all": "pnpm --parallel test",
    "lint": "pnpm --filter frontend lint",
    "format": "pnpm --filter frontend format",
    "clean": "rm -rf apps/*/dist apps/*/node_modules packages/*/dist packages/*/node_modules && pnpm install",
    "db:generate": "pnpm --filter backend db:generate",
    "db:push": "pnpm --filter backend db:push",
    "db:migrate": "pnpm --filter backend db:migrate"
  },
  "keywords": [
    "peru",
    "local-discovery",
    "places",
    "maps",
    "astro",
    "react",
    "typescript"
  ],
  "author": "QueHacer.pe",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "playwright": "^1.57.0"
  }
}